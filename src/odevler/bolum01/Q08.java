package odevler.bolum01;

public class Q08 {
    public static void main(String[] args) {
        double radius = 5.5;
        double perimeter = 2 * radius * Math.PI;
        double area = radius * radius * Math.PI;

        System.out.printf("An circle which has %f radius Calculated value area is: %f, perimeter: %f", radius, area, perimeter);
    }
}
